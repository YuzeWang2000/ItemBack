# ItemBack 开发约束

## UI 变更

任何新增或修改用户界面的工作，都必须先阅读并遵守
[`docs/UI_DESIGN.md`](docs/UI_DESIGN.md)。该文档是 ItemBack 的 UI 设计基线。

- 优先保持界面简洁、克制、清晰和视觉一致，不以堆叠控件的方式换取功能数量。
- 先复用 `apps/web/src/components/ui.tsx` 与 `apps/web/src/styles.css` 中的现有组件、变量和交互模式，再考虑引入新模式。
- 新功能必须覆盖加载、空、错误、成功/完成、禁用等相关状态，并在桌面和窄屏下保持清晰可用。
- 不得为了“先做出来”而引入与现有信息层级、色彩、字体、间距、圆角、按钮层级或导航结构冲突的临时 UI。
- 如果需求会明显破坏主要任务流、造成界面拥挤或引入不一致的视觉/交互语言，暂停实现；明确指出冲突页面与原则，提出更简洁的替代方向，待功能重新设计后再继续。

UI 工作完成前，应按 `docs/UI_DESIGN.md` 的验收清单进行自检，并运行与改动范围相称的自动化检查。

## 文档与运行流程

任何影响用户可见功能、操作步骤、命令、配置、环境变量、端口、数据结构、存储方式、部署方式或子系统生命周期的变更，都必须在同一个变更中同步更新文档。不得把文档更新留给后续任务。

- [`docs/OPERATIONS.md`](docs/OPERATIONS.md) 是启动、停止、更新、备份、状态检查和 Apple Vision 助手生命周期的操作基线；`README.md` 提供入口和简要说明，其他专题文档不得与其冲突。
- 用户步骤必须明确区分首次安装、日常启动、正常停止和功能更新，并说明命令在哪个目录执行、成功后如何确认、失败会影响什么，以及数据是否会保留。
- 每个独立子系统都必须说明启动方式、停止方式、依赖条件和不可用时的影响。当前至少包括 PostgreSQL、API、Web 和 macOS Apple Vision Swift 助手。
- 新增或修改 `package.json` 脚本、Compose 服务、Shell 脚本、环境变量、默认端口、migration 或数据目录时，必须检查并更新 `docs/OPERATIONS.md`、`.env.example` / `.env.production.example` 和相关专题文档。
- 文档只能引用仓库中真实存在且已核对的命令；不得记录依赖个人用户名、机器路径或未实现的恢复/自动化能力。
- 功能更新完成前，应更新 `README.md` 的能力清单或相关用户文档，并运行 `pnpm docs:check` 以及与改动范围相称的自动化检查。

如果一个变更尚无法给出安全、完整且可验证的升级或运行步骤，应暂停交付，先补齐脚本、迁移策略或文档，再继续发布。
