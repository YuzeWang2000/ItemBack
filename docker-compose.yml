services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-itemback}
      POSTGRES_USER: ${POSTGRES_USER:-itemback}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-itemback-dev-only}
    ports:
      - '5432:5432'
    volumes:
      - itemback_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-itemback} -d ${POSTGRES_DB:-itemback}']
      interval: 5s
      timeout: 5s
      retries: 12

volumes:
  itemback_postgres:
